x-logging-defaults: &logging-defaults
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

networks:
  chains:
    driver: bridge
    ipam:
     config:
       - subnet: ${CHAINS_SUBNET:-192.168.0.0/26}
