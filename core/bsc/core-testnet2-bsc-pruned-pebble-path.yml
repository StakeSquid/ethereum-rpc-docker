

services:
  core-testnet2:
    image: docker pull ghcr.io/coredao-org/core-chain:1.0.15    
    sysctls:
      net.ipv4.tcp_slow_start_after_idle: 0
      net.ipv4.tcp_no_metrics_save: 1
    user: root
    ports:
      - "10180:10180"
      - "10180:10180/udp"
    expose:                                                                              
      - "8545"                                                                           
    volumes:                                                                             
      - core-testnet2-bsc-pruned-pebble-path:/bsc/.ethereum
      - /slowdisk:/slowdisk
      - ./core-chain/testnet2:/config      
    entrypoint: [ "geth" ]
    command:
      - --testnet2
      - --config=/config/config.toml
      - --db.engine=pebble
      - --state.scheme=path
      - --syncmode=snap
      - --gcmode=full
      - --rpc.gascap=600000000
      - --nat=extip:${IP}
      - --http
      - --http.addr=0.0.0.0
      - --http.port=8545
      - --http.vhosts=*
      - --http.api=eth,net,web3,txpool,parlia,debug,admin
      - --ws
      - --ws.addr=0.0.0.0
      - --ws.port=8545
      - --ws.origins=*
      - --ws.api=net,web3,eth,debug
      - --maxpeers=200
      - --nodiscover=false
      - --discovery.port=10180     
      - --port=10180
      
      
    restart: unless-stopped
    stop_grace_period: 3m
    networks: 
      - chains
    labels:                                                                              
      - "traefik.enable=true"                                                            
      - "traefik.http.middlewares.core-testnet2-bsc-pruned-pebble-path-stripprefix.stripprefix.prefixes=/core-testnet2"             
      - "traefik.http.services.core-testnet2-bsc-pruned-pebble-path.loadbalancer.server.port=8545"                        
      - "traefik.http.routers.core-testnet2-bsc-pruned-pebble-path.entrypoints=websecure"                                 
      - "traefik.http.routers.core-testnet2-bsc-pruned-pebble-path.tls.certresolver=myresolver"                           
      - "traefik.http.routers.core-testnet2-bsc-pruned-pebble-path.rule=Host(`$DOMAIN`) && PathPrefix(`/core-testnet2`)"            
      - "traefik.http.routers.core-testnet2-bsc-pruned-pebble-path.middlewares=core-testnet2-bsc-pruned-pebble-path-stripprefix, ipwhitelist"

volumes:
  core-testnet2-bsc-pruned-pebble-path:
