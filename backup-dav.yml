version: '3.1'

services:
  backup-dav:
    image: hurlenko/webdav:latest
    restart: unless-stopped
    environment:
      - "WEBDAV_AUTH=false"
    volumes:
      - /backup:/webdav
    labels:                     
      - "traefik.enable=true"
      - "traefik.http.middlewares.backup-server-stripprefix.stripprefix.prefixes=/dev/null"                      
      - "traefik.http.services.backup-server.loadbalancer.server.nport=80"
      - "traefik.http.routers.backup-server.entrypoints=websecure"                                              
      - "traefik.http.routers.backup-server.tls.certresolver=myresolver"                                        
      - "traefik.http.routers.backup-server.rule=Host(`$DOMAIN`) && PathPrefix(`/dev/null`)"                     
      - "traefik.http.routers.backup-server.middlewares=backup-server-stripprefix"                           
    networks:                                                                                         
      - chains
    

