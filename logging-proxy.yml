services:
  logging-proxy:
    build: ./logging-proxy
    networks:
      - chains
    expose:
      - 8545
    environment:
      TARGET_URL: "${LOGGING_PROXY_TARGET_URL}"
      IGNORE_ERROR_METHODS: "${LOGGING_PROXY_IGNORE_ERROR_METHODS}"
